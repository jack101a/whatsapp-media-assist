{
  "name": "media-assist-monorepo",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev:extension": "pnpm --filter media-assist-extension dev",
    "build:extension": "pnpm --filter media-assist-extension build",
    "dev:server": "cd apps/server && uvicorn app.main:app --reload",
    "test:server": "cd apps/server && pytest tests/"
  }
}
